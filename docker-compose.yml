services:
  icp-site:
    build: .
    image: ghcr.io/patchourinya/nas-icp-static-site:latest
    container_name: icp-site
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./data:/data
